pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{ts,tsx}'
      run: npm run check:eslint {staged_files}
    prettier:
      glob: '*.{ts,tsx}'
      run: npx prettier --cache --check {staged_files}
    tsc:
      glob: '*.{ts,tsx}'
      run: npm run check:tsc
